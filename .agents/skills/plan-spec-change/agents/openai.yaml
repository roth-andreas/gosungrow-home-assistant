interface:
  display_name: "Plan Spec Change"
  short_description: "Review a drift-safe, read-only spec plan"
  default_prompt: "Use $plan-spec-change to produce an exact read-only approval plan for my requested behavior."
